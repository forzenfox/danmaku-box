# 抽屉 V3（分组水平化 + 行内按钮精简）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将面板分组导航改为顶部单行横向 chip（溢出滚动/拖拽），并把弹幕行内操作精简为 hover 浮现的铅笔图标，空内容条目点击直接编辑。

**Architecture:** 数据/渲染分离——分组导航构造成 `buildNavItems` 纯函数（可单测），`panel.ts` 只负责把 spec 渲染为 chip DOM；行内操作从"回填+编辑两个文字按钮"收缩为"单击内容回填 + hover 铅笔图标"。横滚用原生 `overflow-x:auto` 兜底，叠加 pointer 拖拽平移增强。

**Tech Stack:** TypeScript 5、Chrome MV3、无框架（原生 DOM）、node:test 单测、esbuild/prettier/eslint。

**规格依据:** `docs/superpowers/specs/2026-09-02-drawer-ui-v3-design.md`（§5 变更清单 / §6 测试策略）。

***

## 文件结构

| 文件                                               | 动作 | 职责                                              |
| ------------------------------------------------ | -- | ----------------------------------------------- |
| `code/danmaku-box/src/panel/nav-items.ts`        | 新建 | `buildNavItems` 纯函数：分组 → 有序导航 chip spec（不含 DOM） |
| `code/danmaku-box/tests/panel/nav-items.test.ts` | 新建 | `buildNavItems` 契约测试                            |
| `code/danmaku-box/src/panel/panel.ts`            | 修改 | renderNav 重写为横滚 chip、拖拽平移、行内按钮精简、siteStatus 清理  |
| `code/danmaku-box/src/panel/panel.css`           | 修改 | chip 样式、横向滚动容器、拖拽 cursor、✎ 图标 hover/聚焦显示        |

所有 npm 命令在 `code/danmaku-box` 目录内执行（cwd: `D:\File\workSpace\AI-test\直播弹幕收藏插件可行性分析\code\danmaku-box`）。

***

### Task 1: buildNavItems 纯函数 + 测试

**Files:**

- Create: `code/danmaku-box/src/panel/nav-items.ts`

- Create: `code/danmaku-box/tests/panel/nav-items.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// danmaku-box/tests/panel/nav-items.test.ts
// 契约测试：分组导航水平化（spec V3 §6）。
// 全部弹幕置顶、新建分组置尾、激活态、星标、占位（active/count 语义）。
import test from 'node:test';
import assert from 'node:assert/strict';
import { buildNavItems } from '../../src/panel/nav-items.ts';
import type { GroupWithCount } from '../../src/panel/nav-items.ts';
import { DEFAULT_GROUP_ID } from '../../src/shared/constants.ts';

function g(id: string, name: string, count: number, builtin = false): GroupWithCount {
  return { id, name, order: 0, builtin, count };
}

test('全部弹幕置顶且计数为各组之和', () => {
  const items = buildNavItems([g('a', '分组A', 2), g('b', '分组B', 3)], 'a');
  assert.equal(items[0]?.kind, 'all');
  if (items[0]?.kind !== 'all') return;
  assert.equal(items[0].name, '全部弹幕');
  assert.equal(items[0].count, 5);
});

test('selectedGroupId 为 null 时全部弹幕激活，否则分组激活', () => {
  const all = buildNavItems([g('a', '分组A', 1)], null);
  assert.equal(all[0]?.kind === 'all' && all[0]?.active, true);
  const pick = buildNavItems([g('a', '分组A', 1)], 'a');
  if (pick[0]?.kind !== 'all' || pick[1]?.kind !== 'group') return;
  assert.equal(pick[0].active, false);
  assert.equal(pick[1].active, true);
});

test('默认收藏组标记内置星标', () => {
  const items = buildNavItems([g(DEFAULT_GROUP_ID, '默认收藏', 4, true)], null);
  if (items[1]?.kind !== 'group') return;
  assert.equal(items[1].builtin, true);
});

test('新建分组 marker 恒在末尾', () => {
  const items = buildNavItems([g('a', '分组A', 1), g('b', '分组B', 2)], null);
  const last = items[items.length - 1];
  assert.equal(last?.kind, 'new');
});
```

- [ ] **Step 2: 运行验证失败**

Run: `node --test tests/panel/nav-items.test.ts`
Expected: FAIL（`Cannot find module '../../src/panel/nav-items.ts'`）

- [ ] **Step 3: 写最小实现**

```ts
// danmaku-box/src/panel/nav-items.ts
// 分组导航数据纯构建函数（V3 水平 chip 形态）。
// 职责：将分组列表映射为渲染所需的「全部弹幕 → 分组 → 新建分组」有序 spec。
// 边界：不含 DOM；纯输入输出，可单测。
import type { Group } from '../shared/types.ts';
import { DEFAULT_GROUP_ID } from '../shared/constants.ts';

export interface GroupWithCount extends Group {
  count: number;
}

export type NavItemSpec =
  | { kind: 'all'; id: null; name: string; count: number; active: boolean }
  | {
      kind: 'group';
      id: string;
      name: string;
      count: number;
      active: boolean;
      builtin: boolean;
      group: GroupWithCount;
    }
  | { kind: 'new'; id: null; name: string; count: number; active: boolean };

export function buildNavItems(
  groups: GroupWithCount[],
  selectedGroupId: string | null,
): NavItemSpec[] {
  const total = groups.reduce((sum, grp) => sum + grp.count, 0);
  const items: NavItemSpec[] = [
    { kind: 'all', id: null, name: '全部弹幕', count: total, active: selectedGroupId === null },
  ];
  for (const grp of groups) {
    items.push({
      kind: 'group',
      id: grp.id,
      name: grp.name,
      count: grp.count,
      active: selectedGroupId === grp.id,
      builtin: grp.id === DEFAULT_GROUP_ID,
      group: grp,
    });
  }
  items.push({ kind: 'new', id: null, name: '新', count: 0, active: false });
  return items;
}
```

- [ ] **Step 4: 运行验证通过**

Run: `node --test tests/panel/nav-items.test.ts`
Expected: PASS（4 tests）

- [ ] **Step 5: 提交**

```bash
git add src/panel/nav-items.ts tests/panel/nav-items.test.ts
git commit -m "feat(panel): 新增 buildNavItems 纯函数（分组导航 spec 构建，全部置顶/新建置尾/激活态）"
```

***

### Task 2: renderNav 重写为顶部横滚 chip + 布局改列

**Files:**

- Modify: `code/danmaku-box/src/panel/panel.ts`（第 20-22 行 `GroupWithCount` 定义 → 改为从 nav-items 导入；`renderNav()` 全文；`navItem()` 辅助函数；导入区）

- Modify: `code/danmaku-box/src/panel/panel.css`（`panel-main` / `group-nav` / `nav-item` / `nav-new-group` / `.nav-more` / `.star` / `.nav-input`）

- [ ] **Step 1: 调整导入与类型来源（panel.ts 头部）**

删除 panel.ts 中本地 `interface GroupWithCount extends Group { count: number; }` 定义与末尾类型依赖，改为：

```ts
import { createStorageWatcher } from './storage-watcher.ts';
import { resolveContentClick, toggleChecked } from './list-interaction.ts';
import { buildNavItems } from './nav-items.ts';
import type { GroupWithCount, NavItemSpec } from './nav-items.ts';
import type { Danmaku } from '../shared/types.ts';
```

- [ ] **Step 2: renderNav 重写为横滚 chip**

将 `renderNav()` 全文替换为（`GroupWithCount` 仅用于 `showGroupMenu` 的入参，取自 spec.group）：

```ts
function renderNav(): void {
  navEl.replaceChildren();
  // 创建分组输入态：短路渲染在横滚容器内（置于循环之前）
  if (state.creatingGroup) {
    const wrap = h('div', 'nav-item');
    const input = document.createElement('input');
    input.className = 'nav-input';
    input.placeholder = '分组名（1-12 字）';
    input.maxLength = GROUP_NAME_MAX_LENGTH;
    input.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') void createGroup(input.value);
      if (e.key === 'Escape') {
        state.creatingGroup = false;
        renderNav();
      }
    });
    input.addEventListener('blur', () => {
      if (state.creatingGroup) {
        state.creatingGroup = false;
        renderNav();
      }
    });
    wrap.appendChild(input);
    navEl.appendChild(wrap);
    input.focus();
    return;
  }

  for (const spec of buildNavItems(state.groups, state.selectedGroupId)) {
    if (spec.kind === 'all') {
      const item = chip({ active: spec.active, name: spec.name, count: spec.count });
      item.addEventListener('click', () => {
        state.selectedGroupId = null;
        void selectAndLoad();
      });
      navEl.appendChild(item);
      continue;
    }
    if (spec.kind === 'new') {
      const add = h('div', 'nav-item nav-new-group', '＋ 新建分组');
      add.addEventListener('click', () => {
        state.creatingGroup = true;
        renderNav();
      });
      navEl.appendChild(add);
      continue;
    }

    // 分组 chip：重命名态渲染输入框
    if (state.renamingGroupId === spec.id) {
      const wrap = h('div', 'nav-item');
      const input = document.createElement('input');
      input.className = 'nav-input';
      input.value = spec.name;
      input.maxLength = GROUP_NAME_MAX_LENGTH;
      input.addEventListener('keydown', (e) => {
        if (e.key === 'Enter') void renameGroup(spec.id, input.value);
        if (e.key === 'Escape') {
          state.renamingGroupId = null;
          renderNav();
        }
      });
      input.addEventListener('blur', () => {
        if (state.renamingGroupId === spec.id) {
          state.renamingGroupId = null;
          renderNav();
        }
      });
      wrap.appendChild(input);
      navEl.appendChild(wrap);
      input.focus();
      continue;
    }

    const item = chip({
      active: spec.active,
      name: spec.name,
      count: spec.count,
      star: spec.builtin,
    });
    item.addEventListener('click', () => {
      state.selectedGroupId = spec.id;
      void selectAndLoad();
    });
    const more = document.createElement('button');
    more.className = 'nav-more';
    more.type = 'button';
    more.textContent = '⋮';
    more.title = '分组操作';
    more.addEventListener('click', (e) => {
      e.stopPropagation();
      showGroupMenu(e as MouseEvent, spec.group);
    });
    item.appendChild(more);
    navEl.appendChild(item);
  }
}

function chip(opts: {
  active: boolean;
  name: string;
  count: number;
  star?: boolean;
}): HTMLElement {
  const item = h('div', `nav-item${opts.active ? ' active' : ''}`);
  if (opts.star) item.appendChild(h('span', 'star', '★'));
  item.appendChild(h('span', 'nav-name', opts.name));
  item.appendChild(h('span', 'nav-count', String(opts.count)));
  return item;
}
```

同时删除旧的 `navItem()` 辅助函数（已无引用）。`showGroupMenu` 签名不变（入参仍为 `GroupWithCount`）。

- [ ] **Step 3: 移除旧 creating 分支**

renderNav 新版本已包含创建态短路与「＋ 新建分组」chip，删除旧 `renderNav` 中 `if (state.creatingGroup) { ... } else { const add = h('div', 'nav-new-group', '＋ 新建分组'); ... }` 一整段旧逻辑（含旧的 `nav-new-group` 分支）。

- [ ] **Step 4: panel.css 布局改列 + chip 样式**

将 `panel-main` / `group-nav` / `.nav-item` 等规则替换：

```css
/* ── 主区：顶部横滚分组 + 下方列表（V3 水平化） ── */
.panel-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.group-nav {
  width: 100%;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 8px 12px;
  overflow-x: auto;
  white-space: nowrap;
  border-bottom: 1px solid var(--border);
  cursor: grab;
  user-select: none;
  scrollbar-width: thin;
}

.group-nav.dragging {
  cursor: grabbing;
}

.group-nav::-webkit-scrollbar {
  height: 4px;
}

.group-nav::-webkit-scrollbar-thumb {
  background: var(--border);
  border-radius: var(--radius-full);
}

.nav-item {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  flex: none;
  padding: 4px 10px;
  border: 1px solid var(--border);
  border-radius: var(--radius-full);
  cursor: pointer;
  user-select: none;
  color: var(--text);
  background: #fff;
  font-size: 12px;
}

.nav-item:hover {
  background: var(--bg-hover);
}

.nav-item.active {
  background: var(--bg-active);
  border-color: var(--primary);
  color: var(--primary);
}

.nav-item .nav-name {
  max-width: 96px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.nav-item .nav-count {
  color: var(--text-secondary);
  font-size: 11px;
}

.nav-item .nav-more {
  visibility: hidden;
  border: none;
  background: none;
  cursor: pointer;
  color: var(--text-secondary);
  font-size: 13px;
  padding: 0 2px;
  border-radius: var(--radius);
  line-height: 1;
}

.nav-item:hover .nav-more,
.nav-item:focus-within .nav-more {
  visibility: visible;
}

.nav-item .star {
  color: #f7ba1e;
}

.nav-item.nav-new-group {
  color: var(--primary);
  border-style: dashed;
  border-color: var(--primary);
}

.nav-input {
  width: 140px;
  padding: 3px 8px;
  border: 1px solid var(--primary);
  border-radius: var(--radius-full);
  font-size: 12px;
}
```

- [ ] **Step 5: 类型检查 + 全量测试**

```bash
npm run typecheck
npm test
```

Expected: typecheck PASS；全部既有测试 + nav-items 测试 PASS（回归正常）。

- [ ] **Step 6: 提交**

```bash
git add src/panel/panel.ts src/panel/panel.css
git commit -m "feat(panel): 分组导航水平化为顶部横滚 chip（列布局 + 胶囊样式 + ⋮ hover 浮现）"
```

***

### Task 3: 分组区拖拽平移（鼠标水平拖拽查看）

**Files:**

- Modify: `code/danmaku-box/src/panel/panel.ts`（事件绑定区，约 813-838 行之间；配合已有 `navEl`）

- [ ] **Step 1: 增加拖拽平移与拖动后点击抑制**

在 `navEl` 引用定义（第 73 行附近）后追加状态，并在 `init` 之前（既有事件绑定区）添加事件绑定：

```ts
// ── 分组区拖拽平移（V3.1）──────────────
// pointer 拖拽：按住横向平移 navEl；移动超过阈值后抑制随后的 click，避免误切分组。
let dragActive = false;
let dragStartX = 0;
let dragStartLeft = 0;
let dragMovedAmount = 0;
let suppressNavClick = false;

navEl.addEventListener('pointerdown', (e) => {
  if (e.pointerType === 'mouse' && e.button !== 0) return; // 仅左键
  dragActive = true;
  dragStartX = e.clientX;
  dragStartLeft = navEl.scrollLeft;
  dragMovedAmount = 0;
  navEl.classList.add('dragging');
});

window.addEventListener('pointermove', (e) => {
  if (!dragActive) return;
  const dx = e.clientX - dragStartX;
  navEl.scrollLeft = dragStartLeft - dx;
  dragMovedAmount = Math.max(dragMovedAmount, Math.abs(dx));
});

function endDrag(): void {
  if (!dragActive) return;
  dragActive = false;
  navEl.classList.remove('dragging');
  if (dragMovedAmount > 6) suppressNavClick = true;
}

window.addEventListener('pointerup', endDrag);
window.addEventListener('pointercancel', endDrag);

navEl.addEventListener(
  'click',
  (e) => {
    if (suppressNavClick) {
      e.stopPropagation();
      e.preventDefault();
      suppressNavClick = false;
    }
  },
  true,
);
```

- [ ] **Step 2: 走查验证（无单测，渲染/交互人工确认）**

构建插件并人工走查（走查清单见 Task 5）：分组溢出时按住左键左右平移、结束不误切分组、原生滚动条仍可拖。

- [ ] **Step 3: 提交**

```bash
git add src/panel/panel.ts
git commit -m "feat(panel): 分组区鼠标拖拽横向平移（拖动超阈值抑制 click 防误切）"
```

***

### Task 4: 行内按钮精简（移除回填、铅笔图标、空内容点击编辑）

**Files:**

- Modify: `code/danmaku-box/src/panel/panel.ts`（`renderItem`；`state.siteStatus` 字段；`init()` 的 GET\_SITE\_STATE 查询）

- Modify: `code/danmaku-box/src/panel/panel.css`（`.item-actions` / 新增 `.item-edit-icon`）

- [ ] **Step 1: 移除** **`siteStatus`** **状态与初始化查询**

```diff
-   /** 活动标签页适配状态：ok / no_active_tab / unsupported / adapter_down */
-   siteStatus: null as string | null,
```

`init()` 中删除：

```ts
  // 面板打开即预查站点状态：非直播间/适配失效时「回填」预先置灰（原型 3.4.1）
  const site = await sendMessage<{ tabId: number | null; site: string | null; status: string }>(
    MESSAGES.GET_SITE_STATE,
  );
  if (site.ok && site.data) state.siteStatus = site.data.status;
```

- [ ] **Step 2: renderItem 行内按钮替换为铅笔图标**

将 renderItem 的形如 `if (!state.batchMode) { const actions = ... 回填 ... 编辑 ... }` 整段（以"常态（非批量）行内仅保留回填/编辑"注释为界）替换为：

```ts
  // 常态行内（V3）：回填=单击内容；仅 hover/聚焦浮现 ✎ 编辑图标；移动/删除并入批量栏
  if (!state.batchMode) {
    const actions = h('div', 'item-actions');
    const edit = document.createElement('button');
    edit.type = 'button';
    edit.className = 'item-edit-icon';
    edit.title = '编辑';
    edit.setAttribute('aria-label', '编辑');
    edit.appendChild(pencilIcon());
    edit.addEventListener('click', () => {
      state.editingId = item.id;
      renderList();
    });
    actions.appendChild(edit);
    main.appendChild(actions);
  }
```

并在文件顶部（renderList 之前）增加画笔 SVG 构造：

```ts
function pencilIcon(): SVGElement {
  const NS = 'http://www.w3.org/2000/svg';
  const svg = document.createElementNS(NS, 'svg');
  svg.setAttribute('viewBox', '0 0 16 16');
  svg.setAttribute('width', '13');
  svg.setAttribute('height', '13');
  svg.setAttribute('fill', 'none');
  svg.setAttribute('stroke', 'currentColor');
  svg.setAttribute('stroke-width', '1.6');
  svg.setAttribute('stroke-linecap', 'round');
  svg.setAttribute('stroke-linejoin', 'round');
  const path = document.createElementNS(NS, 'path');
  path.setAttribute('d', 'M11.5 2.5l2 2L6.5 11.5l-3 1 1-3 7-7z');
  svg.appendChild(path);
  return svg;
}
```

- [ ] **Step 3: 空内容弹幕点击直接进入编辑态**

在 `renderItem` 的 content click 监听中，于 `intent === 'toggle-check'` 分支之后、`fillDanmaku` 调用之前插入：

```ts
    } else if (item.content === '') {
      // V3：空内容弹幕无法回填，单击直接进入编辑态（spec §5.2）
      state.editingId = item.id;
      renderList();
    } else {
      void fillDanmaku(item);
    }
```

（即把原 `else { void fillDanmaku(item); }` 改为三分支：toggle-check / 空内容→编辑 / 其余→回填。批量模式下空内容走 toggle-check 分支不受影响。）

- [ ] **Step 4: panel.css 图标按钮样式**

保留现有 `.danmaku-item .item-actions` 规则不变，在其后追加：

```css
.item-actions .item-edit-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 3px;
  border: none;
  background: none;
  border-radius: var(--radius);
  color: var(--text-secondary);
  cursor: pointer;
  visibility: hidden;
}

.danmaku-item:hover .item-edit-icon,
.danmaku-item:focus-within .item-edit-icon {
  visibility: visible;
}
```

并**删除**死规则 `.danmaku-item .item-actions .btn { padding: 2px 8px; font-size: 11px; }`（行内文字按钮已全部移除）。

- [ ] **Step 5: 回归验证**

```bash
npm test
npm run typecheck
```

Expected: 全部 PASS（list-interaction 契约回归：非批量=fill / 批量=toggle-check 不变）。

- [ ] **Step 6: 提交**

```bash
git add src/panel/panel.ts src/panel/panel.css
git commit -m "refactor(panel): 行内按钮精简——移除回填、编辑改铅笔图标（hover 浮现）、空内容条目点击进入编辑态"
```

***

### Task 5: lint/format/check 全量验证

**Files:** 无（验证 + 走查）

- [ ] **Step 1: 静态检查与格式化**

```bash
npm run lint
npm run format
npm run check
```

Expected: lint 无 error；format 收敛（可能会自动格式化部分文件）；`npm run check`（typecheck + test + lint + build）全绿。

若 format 改动了文件，将格式改动一并提交：

```bash
git add -u
git commit -m "style: prettier 格式化收敛（V3 变更相关文件）"
```

- [ ] **Step 2: 浏览器人工走查（走查项来自 spec §6）**

| 走查项         | 预期                                       |
| ----------- | ---------------------------------------- |
| 分组多到溢出      | 单行 chip 横向滚动 + 鼠标拖拽平移；列表不换行              |
| chip 切换分组   | 单击选中并高亮，列表刷新；hover 尾部 ⋮ 可重命名/删除；创建态输入框可用 |
| 行内条目        | 常态无按钮；hover/键盘聚焦行浮现 ✎；点击 ✎ 进入编辑          |
| 空内容弹幕       | 点击直接进入编辑态（不弹 toast、不回填）                  |
| 批量模式        | 单击内容切换勾选；行内无任何操作；顶部批量操作组正常；空内容条目勾选正常     |
| 回填闭合链路      | 单击内容回填 → 0.9s 抽屉自动收起（回归）                 |
| 窄窗 1280x800 | 抽屉宽度不超 340px；顶部 chip 区与列表布局正常            |
| 键盘          | Tab 可达分组 chip 与 ✎ 图标，聚焦即显示               |

- [ ] **Step 3: 更新规格状态为验收完成**

编辑 `docs/superpowers/specs/2026-09-02-drawer-ui-v3-design.md` 顶部状态行：

```diff
- * 状态：**设计已确认**（2026-09-02 视觉伴侣走查，用户全选并确认整体终态，待实现）
+ * 状态：**验收完成**（<实现日期> 走查通过；npm run check 全绿）
```

- [ ] **Step 4: 提交**

```bash
git add docs/superpowers/specs/2026-09-02-drawer-ui-v3-design.md
git commit -m "docs: 抽屉 V3 验收完成并更新规格状态"
```

***

## Self-Review

- **Spec 覆盖：** §5.1 分组水平化 → Task 1/2/3；§5.2 行内精简（移除回填=Task4 步2、图标化=Task4 步2、空内容点击编辑=Task4 步3、resolveContentClick 不变=Task4 步5 回归）；§5.3 无障碍（title/aria-label=Task4 步2、focus-within 显示=Task4 步4、拖拽阈值=Task3）；§6 测试（buildNavItems 四断言=Task1；list-interaction/drawer-host 回归=Task2 步5）；§7 里程碑 V3.1=Task1-3、V3.2=Task4、V3.3=Task5。

- **无占位符：** 所有步骤含完整可执行代码与命令。

- **类型一致性：** `GroupWithCount` 从 nav-items.ts 导出统一消费；`buildNavItems` 返回 `NavItemSpec[]`，renderNav 严格按 kind 分支；`chip()` 签名与旧 `navItem()` 不共存（后者删除）；`showGroupMenu` 入参沿用 `GroupWithCount`（spec.group）。

