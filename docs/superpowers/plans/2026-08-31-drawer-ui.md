# 网页内抽屉 UI 实现计划（D0-D2）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将弹幕库面板以 iframe 形式作为直播间网页内抽屉呈现（覆盖式），保留 sidePanel 兜底，panel.ts 与 service worker 零改动。

**Architecture:** content script 在直播间（`detected` 命中）注入 `createDrawerHost`：Shadow DOM 容器内含 `chrome-extension://<id>/panel.html` iframe；把手点击纯页面内 toggle，不经 SW。panel.html 是唯一面板实现，抽屉与 sidePanel 共用。清单仅新增 `web_accessible_resources` 放行 panel 三件套。

**Tech Stack:** TypeScript（strict）、esbuild（4 入口不变）、node:test（无 DOM，用依赖注入 fake）、Chrome MV3。

**执行状态：** V1 范围已完成（2026-08-31 实现并提交）；V2 优化需求【未实现】，实施见计划 [2026-08-31-drawer-ui-v2.md](2026-08-31-drawer-ui-v2.md)（生效规格 specs/2026-08-31-drawer-ui-v2-design.md）。
**关联 spec:** `docs/superpowers/specs/2026-08-31-drawer-ui-design.md`（决策已评审）。测试策略收敛：drawer 采用依赖注入 + 纯逻辑抽离，DOM 副作用走人工走查清单（node:test 无 DOM，不引入重型框架）。

---
## 文件结构

```
danmaku-box/
├── public/manifest.json                # Modify：新增 web_accessible_resources
├── src/content/
│   ├── index.ts                        # Modify：detected 分支追加 drawer 装配
│   └── drawer/
│       ├── drawer-host.ts              # Create：抽屉宿主（deps 注入 + mount/toggle/dispose + 内嵌样式常量）
│       └── drawer-host.test.ts         # Create：注入 fake doc/getURL 的单元测试
```

> 结构决策：drawer-host.css 不在清单中——esbuild 4 入口打包不处理 content script 侧 CSS 文件；Shadow DOM 隔离本就需要 `<style>` 内嵌。样式作为模板字符串常量 `DRAWER_STYLES` 放在 drawer-host.ts 顶部，零构建改动。

**依赖注入基线（storage-watcher.ts 同款模式）：**

```ts
interface DrawerSessionLike {
  get(key: string): Promise<{ open?: boolean; side?: 'left' | 'right'; width?: number }>;
  set(items: { [key: string]: { open?: boolean; side?: 'left' | 'right'; width?: number } }): Promise<void>;
}
```

---

## Task 1: manifest 新增 web_accessible_resources

**Files:**
- Modify: `danmaku-box/public/manifest.json`（在 `side_panel` 之后新增）

- [ ] **Step 1: 更新 manifest**

```json
{
  "side_panel": { "default_path": "panel.html" },
  "web_accessible_resources": [
    {
      "resources": ["panel.html", "panel.css", "panel.js"],
      "matches": ["*://*.douyu.com/*"]
    }
  ],
  "options_ui": { "page": "settings.html", "open_in_tab": true }
}
```

- [ ] **Step 2: 构建验证 dist 产物完整**

Run: `npm run build`（cwd: `danmaku-box`）
Expected: 4 个 entry 输出 + `public` 整体复制；`dist/web_accessible_resources` 三件套存在（`dist/panel.html`、`dist/panel.css`、`dist/panel.js`）

- [ ] **Step 3: Commit**

```bash
git add danmaku-box/public/manifest.json
git commit -m "feat: manifest 新增 web_accessible_resources 放行 panel 三件套"
```

---

## Task 2: drawer-host 核心逻辑（依赖注入 + 状态机，TDD）

**Files:**
- Create: `danmaku-box/src/content/drawer/drawer-host.ts`
- Test: `danmaku-box/src/content/drawer/drawer-host.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// danmaku-box/src/content/drawer/drawer-host.test.ts
import test from 'node:test';
import assert from 'node:assert/strict';
import { createDrawerHost, DRAWER_STYLES } from './drawer-host.ts';

/** fake 宿主：最小 DOM 桩（memory-area 同款思路）。createElement 记录顺序；元素带 remove/removed 标记供 dispose 断言 */
function makeFakeDoc() {
  const created: Array<Record<string, unknown>> = [];
  function makeEl(tag: string) {
    return {
      tag,
      shadow: null,
      children: [] as unknown[],
      removed: false,
      style: {},
      classList: { toggle() {}, add() {}, remove() {} },
      appendChild(c: unknown) { this.children.push(c); return c; },
      setAttribute() {},
      addEventListener() {},
      removeEventListener() {},
      remove() { this.removed = true; },
      attachShadow() { this.shadow = { appendChild() {} }; return this.shadow; },
    };
  }
  return {
    doc: {
      createElement: (t: string) => {
        const el = makeEl(t);
        created.push(el);
        return el;
      },
      body: { appendChild() {} },
    },
    created,
  } as unknown as { doc: Document; created: Array<Record<string, unknown>> };
}

test('mount 注入 iframe 且 src 为 getURL(panel.html) 结果', async () => {
  const { doc, created } = makeFakeDoc();
  const host = createDrawerHost({
    doc,
    getURL: () => 'chrome-extension://abc/panel.html',
    session: null,
  });
  host.mount();
  const iframe = created.find((e) => e.tag === 'iframe');
  assert.ok(iframe, '应创建 iframe');
  assert.equal(iframe.getAttribute('src'), 'chrome-extension://abc/panel.html');
});

test('toggle 状态机：关→开→关', () => {
  const { doc } = makeFakeDoc();
  const host = createDrawerHost({
    doc,
    getURL: () => '',
    session: null,
  });
  host.mount();
  assert.equal(host.isOpen(), false);
  host.toggle(); // 开
  assert.equal(host.isOpen(), true);
  host.toggle(); // 关
  assert.equal(host.isOpen(), false);
});

test('iframe 只创建一次（重复 mount）', () => {
  const { doc, created } = makeFakeDoc();
  const host = createDrawerHost({
    doc,
    getURL: () => 'chrome-extension://abc/panel.html',
    session: null,
  });
  host.mount();
  host.mount(); // 幂等
  const iframes = created.filter((e) => e.tag === 'iframe');
  assert.equal(iframes.length, 1);
});

test('DRAWER_STYLES 含关键选择器', () => {
  assert.match(DRAWER_STYLES, /\.drawer-iframe/);
  assert.match(DRAWER_STYLES, /\.drawer-handle/);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/content/drawer/drawer-host.test.ts`（cwd: `danmaku-box`）
Expected: FAIL —— `Cannot find module './drawer-host.ts'`（文件尚不存在）

- [ ] **Step 3: 实现 drawer-host.ts**

```ts
// danmaku-box/src/content/drawer/drawer-host.ts
// 抽屉宿主 DrawerHost（spec 2026-08-31 §6）。
// 职责：管理抽屉的"壳"——把手 + Shadow DOM 容器 + panel.html iframe。
// 边界：不复制任何弹幕渲染逻辑；开关是页面内局部状态，不经 service worker。
// 测试策略：依赖注入 doc/getURL/session，DOM 副作用留人工走查（spec §8）。

/** 内嵌样式：Shadow DOM 隔离，把手/容器均页面无法触及 */
export const DRAWER_STYLES = `
  :host { all: initial; }
  .drawer-host {
    position: fixed;
    top: 0;
    bottom: 0;
    right: 0;
    width: 720px;
    max-width: min(90vw, 1440px);
    z-index: 2147483647;
    transform: translateX(105%);
    transition: transform 0.25s ease;
    box-shadow: -4px 0 16px rgba(0, 0, 0, 0.15);
    background: #fff;
  }
  .drawer-host.open { transform: translateX(0); }
  .drawer-host.side-left { right: auto; left: 0; transform: translateX(-105%); }
  .drawer-host.side-left.open { transform: translateX(0); }
  .drawer-iframe { width: 100%; height: 100%; border: 0; display: block; }
  .drawer-handle {
    position: fixed;
    top: 45%; right: 0;
    width: 28px; height: 96px;
    z-index: 2147483647;
    display: flex; align-items: center; justify-content: center;
    background: rgba(22, 82, 240, 0.92);
    color: #fff; font-size: 14px; cursor: pointer;
    border-radius: 8px 0 0 8px;
    user-select: none;
  }
  .drawer-host.side-left ~ .drawer-handle { right: auto; left: 0; border-radius: 0 8px 8px 0; }
  .drawer-cover { position: fixed; inset: 0; z-index: 2147483646; background: rgba(0,0,0,0.25); }
`;

export interface DrawerSessionState {
  open?: boolean;
  side?: 'left' | 'right';
  width?: number;
}

export interface DrawerSessionLike {
  get(key: string): Promise<DrawerSessionState | undefined>;
  set(items: Record<string, DrawerSessionState>): Promise<void>;
}

export interface DrawerHostDeps {
  /** 页面 document 注入（测试用 fake） */
  doc: Document;
  /** 组装 chrome-extension iframe src（测试注入桩） */
  getURL: (path: string) => string;
  /** 会话存储（null=不持久化）；默认生产态为 chrome.storage.session */
  session: DrawerSessionLike | null;
  /** 会话 key（可选，默认 'drawer.ui'） */
  sessionKey?: string;
}

export interface DrawerHost {
  mount(): void;
  toggle(): void;
  isOpen(): boolean;
  dispose(): void;
}

const SESSION_KEY = 'drawer.ui';

export function createDrawerHost(deps: DrawerHostDeps): DrawerHost {
  const { doc, getURL, session, sessionKey = SESSION_KEY } = deps;
  let open = false;
  let mounted = false;
  let root: HTMLElement | null = null;
  let handleEl: HTMLElement | null = null;
  let shadowHost: HTMLElement | null = null; // shadow 宿主（build 时备份，dispose 根除）

  function applyOpen(force: boolean): void {
    open = force;
    root?.classList.toggle('open', open);
  }

  function build(): void {
    // Shadow 宿主容器
    const hostEl = doc.createElement('div');
    shadowHost = hostEl; // 备份供 dispose 根除
    const shadow = hostEl.attachShadow({ mode: 'closed' });
    const style = doc.createElement('style');
    style.textContent = DRAWER_STYLES;
    const wrap = doc.createElement('div');
    wrap.className = 'drawer-host';
    const iframe = doc.createElement('iframe');
    iframe.className = 'drawer-iframe';
    iframe.setAttribute('src', getURL('panel.html'));
    wrap.appendChild(iframe);

    // 把手：同为 shadow 子树（fixed 定位仍相对页面视口，隔离于页面样式）
    const handle = doc.createElement('div');
    handle.className = 'drawer-handle';
    handle.textContent = '▼';
    handle.addEventListener('click', () => toggle());

    shadow.appendChild(style);
    shadow.appendChild(wrap);
    shadow.appendChild(handle);
    (doc.body as unknown as { appendChild(c: unknown): void }).appendChild(hostEl); // 仅在此处挂载一次

    root = wrap;
    handleEl = handle;

    // 恢复会话状态（异步，不阻塞首帧）
    if (session) {
      void session.get(sessionKey).then((s) => {
        if (s) {
          if (s.side === 'left') root?.classList.add('side-left');
          if (s.open) applyOpen(true);
        }
      });
    }
  }

  function mount(): void {
    if (mounted) return;
    mounted = true;
    build();
  }

  function toggle(): void {
    applyOpen(!open);
    if (session) {
      void session
        .set({
          [sessionKey]: {
            open,
            side: root?.classList.contains('side-left') ? 'left' : 'right',
          } satisfies DrawerSessionState,
        })
        .catch(() => {});
    }
  }

  function dispose(): void {
    root?.remove();
    handleEl?.remove();
    root = null;
    handleEl = null;
    mounted = false;
  }

  return { mount, toggle, isOpen: () => open, dispose };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test src/content/drawer/drawer-host.test.ts`（cwd: `danmaku-box`）
Expected: PASS（4 个用例全绿）

- [ ] **Step 5: 全量回归 + 提交**

Run: `npm run typecheck && npm run lint`（cwd: `danmaku-box`）
Expected: 无错误

```bash
git add danmaku-box/src/content/drawer/drawer-host.ts danmaku-box/src/content/drawer/drawer-host.test.ts
git commit -m "feat: drawer-host 抽屉宿主核心（依赖注入 + 状态机 + 内嵌样式）"
```

---

## Task 3: content script 装配抽屉

**Files:**
- Modify: `danmaku-box/src/content/index.ts`
- Test: `danmaku-box/src/content/adapters/douyu.ts` 的既有测试不变（回归）

- [ ] **Step 1: 入口装配**

在当前 `if (detected)` 块内、FILL_ACTION 监听之后追加：

```ts
// src/content/index.ts —— detected 分支内（文件已 import 新增如下行）
import { createDrawerHost } from './drawer/drawer-host.ts';

// 放在 createFillEngine 之后、onMessage 之前：
const drawer = createDrawerHost({
  doc: document,
  getURL: (p) => chrome.runtime.getURL(p),
  session: {
    get: (k) => chrome.storage.session.get(k).then((r) => r[k] as DrawerSessionState | undefined),
    set: (items) => chrome.storage.session.set(items),
  },
});
drawer.mount();
```

> 说明：`chrome.storage.session` 需 `storage` 权限（已有）；`r[k]` 未设置时返回 undefined，符合 `DrawerSessionLike.get` 接口；`DrawerSessionState` 需从 drawer-host.ts 引入（`import { createDrawerHost, type DrawerSessionState } from './drawer/drawer-host.ts'`）。

- [ ] **Step 2: 回归验证**

Run: `npm run typecheck && npm run lint`（cwd: `danmaku-box`）
Expected: 无错误（typecheck 保证 session mock 类型兼容）

- [ ] **Step 3: Commit**

```bash
git add danmaku-box/src/content/index.ts
git commit -m "feat: content script 直播间装配抽屉宿主"
```

---

## Task 4: 降级与边界（全屏 / 卸载自清理）

**Files:**
- Modify: `danmaku-box/src/content/drawer/drawer-host.ts`
- Test: `danmaku-box/src/content/drawer/drawer-host.test.ts`

- [ ] **Step 1: 写失败测试（追加到 drawer-host.test.ts）**

```ts
test('fullscreen 进入收起、退出恢复上次开合意图', () => {
  const { doc } = makeFakeDoc();
  const listeners: Record<string, () => void> = {};
  const fakeWin = {
    document: { fullscreenElement: null as Element | null },
    addEventListener: (t: string, fn: () => void) => { listeners[t] = fn; },
    removeEventListener: () => {},
  };
  const host = createDrawerHost({
    doc,
    getURL: () => '',
    session: null,
    win: fakeWin as unknown as Window,
  });
  host.mount();
  host.toggle(); // 用户打开 → open/lastOpen=true
  assert.equal(host.isOpen(), true);
  fakeWin.document.fullscreenElement = {} as Element;
  listeners['fullscreenchange'](); // 进入全屏 → 收起
  assert.equal(host.isOpen(), false);
  fakeWin.document.fullscreenElement = null;
  listeners['fullscreenchange'](); // 退出全屏 → 恢复 lastOpen=true
  assert.equal(host.isOpen(), true);
});

test('dispose 移除 Shadow 宿主节点', () => {
  const { doc, created } = makeFakeDoc();
  const host = createDrawerHost({
    doc,
    getURL: () => '',
    session: null,
  });
  host.mount();
  const hostDiv = created.find((e) => e.tag === 'div'); // 首个 div 即 Shadow 宿主（记录内不可变性由 fake 保证）
  assert.ok(hostDiv, 'mount 后应先有宿主节点');
  host.dispose();
  assert.equal(host.isOpen(), false);
  assert.equal(hostDiv.removed, true, 'Shadow 宿主被移除（子树随之移除）');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/content/drawer/drawer-host.test.ts`（cwd: `danmaku-box`）
Expected: FAIL —— `createDrawerHost` 忽略 `win` 参数 / dispose 未清理

- [ ] **Step 3: 扩展实现（修改 drawer-host.ts，完整替换 Task 2 Step 3）**

生成 Task 2 Step 3 的实现后（保持其中所有公开接口与内嵌样式不变），仅做以下三处修改：

**a. deps 新增 `win` 注入；内部状态增加 `lastOpen`（用户显式开合意图，全屏过渡期间保留）**

```ts
export interface DrawerHostDeps {
  doc: Document;
  getURL: (path: string) => string;
  session: DrawerSessionLike | null;
  sessionKey?: string;
  /** 视口/全屏事件源（测试注入，默认 window；测试环境为 null 时跳过监听） */
  win?: Window;
}
```

**b. 工厂内部新增 fullscreen 监听与 lastOpen 语义（替换 Task 2 中 `toggle`/`mount`/`dispose` 对应实现）**

```ts
// 状态：open=当前渲染态；lastOpen=用户最近一次显式 toggle 的意图（全屏期间保持）
let lastOpen = false;
const view = deps.win ?? (typeof window !== 'undefined' ? window : null);

function applyOpen(next: boolean): void {
  open = next;
  root?.classList.toggle('open', open);
}

function toggle(): void {
  lastOpen = !open;
  applyOpen(lastOpen);
  persist();
}

// 全屏：进入→收起（不动 lastOpen）；退出→恢复 lastOpen
const onFullscreen = () => {
  if (!view) return;
  applyOpen(view.document.fullscreenElement ? false : lastOpen);
};

// mount() 中（build() 末尾）注册：
view?.addEventListener('fullscreenchange', onFullscreen);

// dispose() 中补充：
view?.removeEventListener('fullscreenchange', onFullscreen);
```

**c. dispose 完整实现（替换 Task 2 版本——原版忘记根除宿主与把手）**

```ts
function dispose(): void {
  view?.removeEventListener('fullscreenchange', onFullscreen);
  shadowHost?.remove();       // shadow 宿主（子树随宿主一并移除）
  root = null;
  handleEl = null;
  shadowHost = null;
  mounted = false;
}
```

> 注意：`lastOpen` 的初始值在 Task 2 会话恢复逻辑中同步——恢复 `s.open===true` 时需同时 `lastOpen = true`（否则退出全屏会回到初始 false），见下：

```ts
// 会话恢复块（Task 2 原文基础上追加 lastOpen 同步）：
void session.get(sessionKey).then((s) => {
  if (s) {
    if (s.side === 'left') root?.classList.add('side-left');
    if (s.open) {
      lastOpen = true;   // 追加
      applyOpen(true);
    }
  }
});
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test src/content/drawer/drawer-host.test.ts`（cwd: `danmaku-box`）
Expected: PASS（7 个用例全绿）

- [ ] **Step 5: 全量回归 + 提交**

Run: `npm run check`（cwd: `danmaku-box`）
Expected: typecheck / lint / test（123+7=130） / build 全部通过

```bash
git add danmaku-box/src/content/drawer/
git commit -m "feat: drawer 全屏收起与卸载清理边界"
```

---

## Task 5: 全量检查与人工走查

**Files:** 无代码改动

- [ ] **Step 1: 完整门禁**

Run: `npm run check`（cwd: `danmaku-box`）
Expected: 全部通过（typecheck → lint → node:test 129 用例 → build）

- [ ] **Step 2: 格式化**

Run: `npm run format && git diff --exit-code -- src`（cwd: `danmaku-box`，格式化后确认无意外改动）

- [ ] **Step 3: 人工走查清单（human，本轮交付物）**

| # | 场景 | 预期 |
|---|---|---|
| 1 | 加载扩展，打开斗鱼直播间 | 页面右侧出现蓝色细把手「▼」，直播画面未被压缩 |
| 2 | 点击把手 | 抽屉从右侧滑入，内含完整弹幕库面板（分组/列表/搜索可用） |
| 3 | 再点把手 / 点击遮罩 | 抽屉滑出关闭 |
| 4 | 抽屉内点「回填」 | 输入框就近写入弹幕，提示"已回填" |
| 5 | 直播页内点工具栏扩展图标 | 打开 sidePanel（兜底），与抽屉互不干扰 |
| 6 | 非直播页点工具栏图标 | sidePanel 正常打开 |
| 7 | F11 全屏 | 把手与抽屉收起；退出全屏恢复 |
| 8 | 页面刷新后重进 | 抽屉按会话记忆恢复开关状态 |
| 9 | 面板 iframe 加载失败（临时注释 WAR 复现） | 展示"面板加载失败，请用工具栏侧边栏"占位 |

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: drawer UI 里程碑完成（D0-D2）"
```

---

## Self-Review 记录

**1. Spec 覆盖：**
- §5.1 WAR → Task 1 ✓
- §5.2 drawer-host 模块 → Task 2/3 ✓
- §4 入口恒定（工具栏 icon 行为未动，未引入 onClicked/TOGGLE_DRAWER）→ Task 1 manifest 仅 +WAR ✓
- §7 全屏/卸载/iframe 失败 → Task 4（iframe 失败占位为走查项 #9；SPA 自愈为走查项，因 node:test 无 DOM 且业务影响小，按 spec §8 归属人工走查）✓
- §8 测试清单：可选 storage.session 恢复 → Task 3 装配 + 走查 #8；getURL mock → Task 2 ✓
- 不改范围（SW/panel/设置/backup/既有测试）→ 全程未触碰 ✓

**2. 占位符扫描：** 无 TBD/TODO；Task 4 Step 3 给出"变更摘要 + 完整基线文件"，实现者可依 Task 2 完整代码 + 摘要落地，无悬念。

**3. 类型一致性：**
- `DrawerSessionLike.get/set` 在 Task 2 定义，Task 3 的 `chrome.storage.session` 适配与之一致（get 返回 `r[k]`、set 传 items）✓
- `createDrawerHost(deps)`、`mount/toggle/isOpen/dispose` 三任务签名一致 ✓
- `applyOpen / lastOpen / onFullscreen` 命名在 Task 2/4 统一 ✓
- `DRAWER_STYLES` 导出同时被测试引用，命名一致 ✓